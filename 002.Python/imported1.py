import re # Regex

re.A
re.A.ASCII
re.A.MULTILINE
re.A.conjugate(
re.A.numerator
re.A.DEBUG
re.A.TEMPLATE
re.A.denominator
re.A.real
re.A.DOTALL
re.A.UNICODE
re.A.from_bytes(
re.A.to_bytes(
re.A.IGNORECASE
re.A.VERBOSE
re.A.imag
re.A.value
re.A.LOCALE
re.A.bit_length(
re.A.name

re.RegexFlag(
re.copyreg
re.search(
    
re.ASCII
# re.ASCII.ASCII
re.ASCII.UNICODE
re.ASCII.name
re.ASCII.DEBUG
re.ASCII.VERBOSE
re.ASCII.numerator
re.ASCII.DOTALL
re.ASCII.bit_length(
re.ASCII.real
re.ASCII.IGNORECASE
re.ASCII.conjugate(
re.ASCII.to_bytes(
re.ASCII.LOCALE
re.ASCII.denominator
re.ASCII.value
re.ASCII.MULTILINE
re.ASCII.from_bytes(  
re.ASCII.TEMPLATE
re.ASCII.imag 

re.S
re.enum
re.split(
re.DEBUG
re.Scanner
re.error(
re.sre_compile
re.DOTALL
re.T
re.escape(
re.sre_parse
re.I
re.TEMPLATE
re.findall(
re.sub(
re.IGNORECASE
re.U
re.finditer(
re.subn(
re.L
re.UNICODE
re.fullmatch(
re.template(
re.LOCALE
re.VERBOSE
re.functools    
re.M
re.X
re.match(       
re.MULTILINE
re.compile
re.purge( 

####################################################################################################################
####################################################################################################################
####################################################################################################################

# import Ordereddict # class # Erro import
import Random

random.BPF
random.choices(
random.random(
random.LOG4
random.expovariate(
random.randrange(
random.NV_MAGICCONST
random.gammavariate(
random.sample(
random.RECIP_BPF
random.gauss(
random.seed(
random.Random(
random.getrandbits(
random.setstate(
random.SG_MAGICCONST
random.getstate(
random.shuffle(
random.SystemRandom(
random.lognormvariate(
random.triangular(
random.TWOPI
random.normalvariate(
random.uniform(
random.betavariate(
random.paretovariate(
random.vonmisesvariate(
random.choice(
random.randint(
random.weibullvariate(
